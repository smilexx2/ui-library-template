# Installation

Get started with @smilexx2 UI Library in your React project.

## Prerequisites

Before installing, ensure your project meets these requirements:

- **Node.js** 18.0 or later
- **React** 18.0 or later (React 19 compatible)
- **React DOM** 18.0 or later

### Required if using shadcn/ui

This library extends shadcn/ui, so you'll need:
- shadcn/ui components installed
- Tailwind CSS configured
- Required shadcn/ui dependencies

## Package Installation

Our library is modular - install only what you need:

### Core Utilities

```bash
npm install @smilexx2/ui-core
```

Provides the `cn` utility function for className management.

### UI Components

```bash
npm install @smilexx2/ui
```

shadcn/ui compatible components with consistent styling.

### Custom Components

```bash
npm install @smilexx2/custom-button
```

Enhanced components with additional features.

### Install Everything

```bash
npm install @smilexx2/ui-core @smilexx2/ui @smilexx2/custom-button
```

## Tailwind CSS Configuration

### For Tailwind CSS v4 (Latest)

If you're using Tailwind CSS v4, add the library paths to your CSS:

```css
/* app.css or globals.css */
@import "tailwindcss";
@import "@smilexx2/ui/styles.css";
@import "@smilexx2/custom-button/styles.css";
```

### For Tailwind CSS v3

Add the library paths to your content configuration:

```js
// tailwind.config.js
module.exports = {
  content: [
    "./src/**/*.{js,ts,jsx,tsx,mdx}",
    "./node_modules/@smilexx2/ui/dist/**/*.{js,cjs}",
    "./node_modules/@smilexx2/custom-button/dist/**/*.{js,cjs}"
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
```

## Import Styles

Don't forget to import the component styles in your app:

```tsx
// In your root component or CSS file
import '@smilexx2/ui/styles.css'
import '@smilexx2/custom-button/styles.css'
```

## Basic Usage

Here's a complete example to get you started:

```tsx
// App.tsx
import React from 'react'
import { cn } from '@smilexx2/ui-core'
import { Button } from '@smilexx2/ui'
import { CustomButton } from '@smilexx2/custom-button'

// Import styles
import '@smilexx2/ui/styles.css'
import '@smilexx2/custom-button/styles.css'

function App() {
  return (
    <div className={cn("min-h-screen p-8")}>
      <h1 className="text-2xl font-bold mb-6">
        My App with @smilexx2 UI
      </h1>
      
      <div className="space-y-4">
        <Button variant="default">
          Standard Button
        </Button>
        
        <CustomButton 
          variant="outline" 
          leftIcon={<span>🚀</span>}
        >
          Enhanced Button
        </CustomButton>
      </div>
    </div>
  )
}

export default App
```

## TypeScript Support

All packages include TypeScript definitions. For the best experience:

```json
// tsconfig.json
{
  "compilerOptions": {
    "moduleResolution": "bundler", // or "node16", "nodenext"
    "allowSyntheticDefaultImports": true,
    "esModuleInterop": true
  }
}
```

## Peer Dependencies

The library has the following peer dependencies that should already be in your project:

```json
{
  "react": "^18.0.0 || ^19.0.0",
  "react-dom": "^18.0.0 || ^19.0.0",
  "@radix-ui/react-slot": "^1.0.0",
  "class-variance-authority": "^0.7.0",
  "clsx": "^2.0.0",
  "tailwind-merge": "^2.0.0"
}
```

## Verification

After installation, verify everything is working:

<LiveCodeBlock
  code={`() => {
  const [status, setStatus] = React.useState('checking')
  
  React.useEffect(() => {
    // Simulate checking installation
    setTimeout(() => setStatus('ready'), 1000)
  }, [])
  
  return (
    <div className="p-6 rounded-lg bg-gray-50 dark:bg-gray-900">
      <h3 className="font-semibold mb-4">Installation Check</h3>
      
      {status === 'checking' ? (
        <div className="flex items-center gap-2 text-gray-600">
          <Spinner size="sm" />
          <span>Checking installation...</span>
        </div>
      ) : (
        <div className="space-y-3">
          <div className="flex items-center gap-2 text-green-600">
            <span>✅</span>
            <span>Components loaded successfully</span>
          </div>
          
          <div className="flex gap-2">
            <CustomButton size="sm" variant="default">
              It works! 🎉
            </CustomButton>
            <CustomButton size="sm" variant="outline">
              Ready to build
            </CustomButton>
          </div>
        </div>
      )}
    </div>
  )
}`}
  className="my-6"
/>

## Troubleshooting

### Module Resolution Issues

If you encounter import errors:

1. Ensure you're using a modern bundler (Vite, Next.js 13+, etc.)
2. Check your `tsconfig.json` moduleResolution setting
3. Try clearing your node_modules and reinstalling

### Style Issues

If styles aren't applying:

1. Verify you've imported the CSS files
2. Check your Tailwind configuration includes the library paths
3. Ensure your bundler processes CSS imports

### TypeScript Errors

If you see type errors:

1. Update TypeScript to 5.0 or later
2. Check that all peer dependencies are installed
3. Restart your TypeScript server in your IDE

## Theme Configuration

### Dark Mode Support

This documentation site and the component library fully support dark mode through multiple detection methods:

#### Nextra Theme Configuration

The docs are configured with automatic theme switching:

```tsx
// layout.tsx
<Layout
  darkMode={true}                    // Enable dark mode toggle
  nextThemes={{                      // next-themes configuration
    defaultTheme: 'system',          // Default to system preference
    storageKey: 'nextra-theme'       // LocalStorage key
  }}
  // ... other layout options
>
```

#### CSS Variables

Our CSS uses comprehensive theme variables that work with multiple dark mode approaches:

```css
/* System preference */
@media (prefers-color-scheme: dark) { /* ... */ }

/* Class-based (next-themes, manual toggle) */
.dark { /* ... */ }

/* Data attribute (compatibility) */
[data-theme="dark"] { /* ... */ }
```

#### Component Integration

Components automatically sync with the current theme using:

- **Direct integration with next-themes** - Components use `useTheme()` hook for real-time theme detection
- **Automatic theme switching** - No manual observers or localStorage listeners needed
- **System preference support** - Respects user's system theme preference
- **Manual theme override** - Supports explicit light/dark mode selection

This ensures seamless compatibility with shadcn/ui, next-themes, and system preferences while providing a clean, maintainable codebase.

## Next Steps

- Explore the [Components](/components) section
- Check out the [Custom Button](/components/button) documentation
- Join our [GitHub Discussions](https://github.com/your-username/ui-library/discussions) for help