# UI Library Documentation

Welcome to the **@smilexx2** UI Library documentation! A collection of React components that extend and complement shadcn/ui with additional features and enhanced functionality.

## Features

- 🚀 **Extends shadcn/ui** - Works seamlessly with your existing shadcn/ui setup
- ⚡ **Modern Stack** - Built with React 19, TypeScript 5.8, and Tailwind CSS v4
- 🎨 **Interactive Documentation** - Live code examples powered by react-live
- 📦 **Modular Packages** - Install only what you need
- 🔧 **Fully Typed** - Complete TypeScript support with IntelliSense
- ♿ **Accessible** - ARIA compliant components with keyboard navigation
- 🎯 **Tree-shakeable** - Optimized bundle sizes with proper ESM/CJS exports

## Quick Start

### Installation

Choose the packages you need:

```bash
# Core utilities
npm install @smilexx2/ui-core

# shadcn/ui style components
npm install @smilexx2/ui

# Enhanced components
npm install @smilexx2/custom-button
```

### Basic Usage

```tsx
import { cn } from '@smilexx2/ui-core'
import { Button } from '@smilexx2/ui'
import { CustomButton } from '@smilexx2/custom-button'

// Import styles
import '@smilexx2/ui/styles.css'
import '@smilexx2/custom-button/styles.css'

function App() {
  return (
    <div className={cn("space-y-4 p-8")}>
      <Button variant="outline">Standard Button</Button>
      <CustomButton variant="default">Enhanced Button</CustomButton>
    </div>
  )
}
```

## Available Packages

| Package | Description | Version |
|---------|-------------|---------|
| `@smilexx2/ui-core` | Core utilities and helpers | 0.1.0 |
| `@smilexx2/ui` | shadcn/ui compatible components | 0.1.0 |
| `@smilexx2/custom-button` | Enhanced button with animations | 0.1.0 |

## Interactive Examples

This documentation includes live, editable code examples. Try modifying the code in any `<LiveCodeBlock>` to see real-time results!

<LiveCodeBlock
  code={`() => {
  const [clicked, setClicked] = React.useState(false)
  
  return (
    <div className="space-y-4">
      <p className="text-sm text-gray-600 dark:text-gray-400">
        {clicked ? '🎉 Welcome to @smilexx2 UI!' : 'Click the button below to get started'}
      </p>
      <CustomButton 
        onClick={() => setClicked(true)}
        variant={clicked ? "default" : "outline"}
      >
        {clicked ? 'Explore Components →' : 'Get Started'}
      </CustomButton>
    </div>
  )
}`}
  className="my-6"
/>

## Why @smilexx2 UI?

- **Extends, not replaces** - Use alongside your existing shadcn/ui components
- **Production ready** - Fully tested components with proper error handling
- **Developer friendly** - Clear documentation with interactive examples
- **Performance focused** - Optimized builds with minimal bundle impact
- **Community driven** - Open source and welcoming contributions

## Next Steps

- [Installation Guide](/installation) - Set up the library in your project
- [Components](/components) - Browse available components
- [Custom Button](/components/button) - See our enhanced button in action

## Contributing

This is a template repository - fork it to create your own component library! Check out our [GitHub repository](https://github.com/your-username/ui-library) for more information.